<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F8F8F8"
    android:padding="16dp"
    android:id="@+id/main">

    <!-- Title -->
    <TextView
        android:id="@+id/tvWaitingRoomTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Waiting Room"
        android:textSize="24sp"
        android:textColor="#333333"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp"
        android:layout_marginTop="24dp"
        android:textStyle="bold" />

    <!-- Room Code -->
    <TextView
        android:id="@+id/roomCode"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Room Code: 123456"
        android:textSize="18sp"
        android:textColor="#555555"
        android:layout_below="@id/tvWaitingRoomTitle"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="16dp" />

    <!-- Player List -->
    <ListView
        android:id="@+id/playersList"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_below="@id/roomCode"
        android:layout_above="@id/startGameButton"
        android:layout_marginTop="16dp"
        android:divider="@android:color/darker_gray"
        android:dividerHeight="1dp"
        android:background="#FFFFFF"
        android:padding="8dp" />

    <!-- Waiting Message -->
    <TextView
        android:id="@+id/waitingMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Waiting for players to join..."
        android:textSize="16sp"
        android:textColor="#888888"
        android:layout_below="@id/roomCode"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="15dp" />

    <!-- Start Game Button -->
    <Button
        android:id="@+id/startGameButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Start Game"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:backgroundTint="#2ECC71"
        android:textColor="#FFFFFF"
        android:padding="12dp"
        android:elevation="4dp"
        android:enabled="false" />

</RelativeLayout>
